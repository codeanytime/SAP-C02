# 1.EKS overview

**EKS** (Elastic Kubernetes Service) là **dịch vụ Kubernetes được quản lý của AWS**, cho phép chạy ứng dụng Kubernetes mà không cần quản lý control plane.

Bao gồm:

- API Server (kube-apiserver)
- etcd
- IAM
- VPC CNI
